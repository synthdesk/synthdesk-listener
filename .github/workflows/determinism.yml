name: determinism-gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk-tools/.github/workflows/golden-gates-reusable.yml@c4592edbbfb3cf18c4a1ddcbdac8b044b6c4fe42
    with:
      repo_role: listener
      python_listener: "3.12"
    secrets: inherit
