name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk-tools/.github/workflows/golden-gates-reusable.yml@2af86d84aade1cc64db032a25626226649bee6de
    with:
      repo_role: listener
      python_listener: "3.12"
    secrets: inherit
