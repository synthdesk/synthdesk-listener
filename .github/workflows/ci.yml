name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  call:
    uses: synthdesk/synthdesk/.github/workflows/golden-gates-reusable.yml@ci-gates-v1.0.1
    with:
      repo_role: listener
      python_listener: "3.12"
    secrets: inherit
